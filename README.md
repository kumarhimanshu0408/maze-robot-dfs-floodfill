# maze-robot-dfs-floodfill
A MATLAB simulation of a wheeled robot that builds its own map using local wall sensing, explores via DFS, computes shortest paths using flood-fill, and navigates using real kinematic control.
